<div class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/"><%= name %></a>
    </div>
    <div class="nav navbar-collapse collapse">
        <div id="greeting" class="nav navbar-left navbar-text" style="margin-top: 0; margin-bottom: 0">
            <% if (user) {%>
               Signed in as <%= user.name %> &nbsp; <a class="btn btn-default navbar-btn" href="/logout">Logout</a>
            <% } else { %>
                Not Signed in &nbsp; <a class="btn btn-primary navbar-btn" href="/logout">Sign in</a>
            <% }%>
        </div>
         <div class="nav navbar-right" >
            <ul class="nav navbar-nav nav-pills">
                <% if (user) {%>
                    <% if (user.admin) {%>
                        <li><a href="/settings">Settings</a></li>
                        <li><a href="/log">Logs</a></li>
                    <% }%>
                    <li><a href="/logout">Logout</a></li>
                <% }%>
            </ul>
        </div>
    </div>
  </div>
</div>